 Децентрализованная Система Ставок (Betting DApp)

## 🌟 Обзор Проекта

Этот проект представляет собой децентрализованное приложение (DApp), построенное на фреймворке **Scaffold-ETH 2**, для заключения пари (ставок) на исходы событий (например, спортивных матчей). Смарт-контракт управляет приемом ставок от пользователей и автоматическим распределением выигрышей после объявления окончательного результата владельцем контракта.

---

## 🛠️ Технологии

* **Solidity:** Язык программирования для смарт-контракта `BettingContract`.
* **Hardhat:** Среда разработки для тестирования и развертывания.
* **Next.js / TypeScript / Tailwind CSS:** Фронтенд для взаимодействия с контрактом.
* **wagmi / RainbowKit:** Для управления кошельками и подключения к блокчейну.

---

## 📜 Детали Смарт-Контракта

Основной смарт-контракт — **`BettingContract.sol`**.

### 🎲 Возможные Исходы (Outcome Enum)

События могут иметь один из следующих результатов:
* `0`: `PENDING` (Ожидание, ставки открыты)
* `1`: `TEAM_A_WINS` (Команда А победила)
* `2`: `TEAM_B_WINS` (Команда Б победила)
* `3`: `DRAW` (Ничья)

### 🧩 Основные Функции

| Функция | Тип | Описание |
| :--- | :--- | :--- |
| `placeBet(Outcome _prediction)` | **`write`** | Принимает ставку (ETH) от пользователя на определенный исход (`1`, `2` или `3`). |
| `announceResult(Outcome _finalOutcome)` | **`write`** (onlyOwner) | Владелец контракта объявляет окончательный результат. Запускает автоматическое распределение выигрышей. |
| `withdrawFunds()` | **`write`** (onlyOwner) | Позволяет владельцу вывести любые средства, оставшиеся в контракте после выплат. |
| `getContractBalance()` | **`read`** | Возвращает текущий баланс контракта (общую сумму ставок). |
| `totalBetsForOutcome(uint8)` | **`read`** | Возвращает общую сумму ставок для указанного исхода. |

---

## 🚀 Начало Работы

### 1. Клонирование и Установка

```bash
git clone YOUR_GITHUB_REPO_URL bahis-dapp
cd bahis-dapp
yarn install

2. Запуск Локального Блокчейна
В новом окне терминала запустите локальный Hardhat блокчейн:
yarn chain

3. Развертывание Контракта
Во втором окне терминала разверните BettingContract:
yarn deploy

4. Запуск Фронтенда
В третьем окне терминала запустите DApp:
yarn start

Тестирование
Запуск Тестов: Все функции контракта (включая выплаты и контроль доступа) протестированы.
yarn test

Интерфейс: Для взаимодействия с функциями placeBet и announceResult перейдите на вкладку Debug Contracts в браузере.

Статус и Готовность
Проект полностью завершен и готов к сдаче. Все 10 тестов были успешно пройдены и код загружен на GitHub.
